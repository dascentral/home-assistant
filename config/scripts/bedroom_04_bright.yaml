bedroom_bright:
  alias: "Bedroom Bright"
  description: "Activates Hue bright scene and turns off overhead."
  sequence:
    - service: scene.turn_on
      target:
        entity_id: scene.hue_bedroom_bright
    - service: light.turn_off
      target:
        entity_id: light.bedroom_overhead
  mode: single
